package es.upsa.mimo.v2021.fitup.utils


class Constants {
    companion object {
        const val APP_TAG = "upsa.mimo.v2021.fitup"
        const val DATE_TIME_FORMAT = "dd/MM/yyyy hh:mm:ss"
        const val DATE_FORMAT = "dd/MM/yyyy"
        const val PASSWORD_PATTERN = "^(?=.*[a-z])(?=.*[A-Z])(?=.*\\d)[a-zA-Z\\d]{3,30}$"

    }
}